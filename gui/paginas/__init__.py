"""Seções da página única — funções construtoras que recebem o ``Workspace``.

Não registram rotas: o app é de página única (ver :mod:`gui.workspace`). Os
módulos ``inicio``/``sistema``/``galeria`` expõem builders chamados pelas regiões
``@ui.refreshable`` do controlador.
"""
